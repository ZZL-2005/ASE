1.sandbox日志细化，比如agent的thinking流，实际接受上下文，tool calling反馈，请求的json模式+实际进入模型的上下文模式

2.memory工具，上下文管理（check一下原本仓库有没有，目前感觉这部分是丢失的）

3.指令说明指导提供的细致一点，方便使用

4.agent的docker image换一下，直接换成ubantu吧，不要python slim

5.这个项目容器来来回回创建又关闭的，我需要一个工具统一管理项目启动的容器，包括其数据卷之类的，不然的话到时候服务器会乱七八糟存一堆东西

6.是否存在多主体建模？目前在genral消息通道内，似乎

7.websearch，要不要暴露真实网络（用本地部署gitlab替代一下）

8.轨迹记录与重放（先搞清楚整个api请求过程，api格式，实际输入模型格式，模型实际输出格式，解析反馈格式，搞清楚轨迹重放到底要记录什么）

9.看一下启动的时候到底咋回事，目前虽然成功启动了，但是真的很慢啊

10.目前copy到agent sandbox里面的东西似乎不太对，要改改

11.email功能

12.启动应该是可以有定制上下文覆写的，比如那几个md什么的，了解一下他们的机制
